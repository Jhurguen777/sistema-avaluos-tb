import { ImportadorJson } from "@/components/importar-json/importador-json"

/**
 * Submódulo de Importación: análisis e importación masiva de JSONs
 * (subidos desde el PC o generados por el submódulo Scraper).
 */
export default function DatosImportarPage() {
  return <ImportadorJson />
}
